任务：处理 GitHub PR #1932 相关的导入 bug，并在主分支上创建 PR（如果确认确实需要代码改动）。

项目：/root/code-space/blade-agent
目标 worktree：由主控提供，必须基于主分支最新状态。

请完整执行：
1. 复核 PR #1932 及此前调查结论：原始 v2608.0.4 单镜像后端已有完整归档导入接口，但旧前端顶部入口仍走 ZIP 代码导入；目标是让完整项目归档导入流程可用。
2. 在基于主分支的独立 worktree 中实现必要修复，遵循仓库规范；不要修改无关功能。
   另外一并修复导入预览弹框的默认目录选择问题：既然用户已经知道/提供了项目目录，预览弹框默认应选中对应的项目目录，而不是 Home 根目录；请定位当前默认值/路径映射逻辑，确保实际项目目录被合理默认选中，并补充回归测试。
3. 补充/调整测试，运行与改动相关的测试及必要检查。
4. 提交代码并推送分支，在主分支上创建 GitHub PR。不要合并。
5. PR 创建后继续按仓库 .claude/skills/babysit-pr/SKILL.md 完整 babysit：检查 CI、Codex review、review threads、冲突；必要时修复并回复/resolve。超过 10 条 review 意见先做方案审计，不要逐条堆补丁。

回报：是否确实需要改动、根因、修改文件、测试结果、commit、PR 链接/编号，以及 babysit 的最终状态。若当前仓库权限或网络导致无法创建 PR，明确报告阻塞点和已完成内容。
