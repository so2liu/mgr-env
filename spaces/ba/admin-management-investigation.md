新任务：为 blade-agent 规划“管理员统一管理页面”，先做需求与现状调查，不修改代码、不创建 PR。

项目：/root/code-space/blade-agent

用户需求：
- 入口位于 Chat 页面左上角，当前已有“创建新对话”和“软件工厂”等入口；在其下增加“管理配置”之类入口。
- 点击后打开新的浏览器标签页。
- 新标签页采用左右布局：左侧目录，右侧正文。
- 当前明确想到的内容是模型配置，其他管理内容暂未确定，希望先调查后提出合理建议。

请 Codex 调查并回报：
1. Chat 页面左上角现有入口的组件、路由和打开新标签页的现有模式，指出最合适的接入点。
2. 仓库已有的管理员/设置/配置页面、权限模型、模型配置相关页面和 API；避免重复建设。
3. 根据项目现状提出统一管理页面首期建议目录（模型配置之外还应有哪些高价值且已有后端/配置支撑的模块），并区分首期必须、可选后续、暂不建议。
4. 给出推荐路由、页面布局组件复用方案、权限/认证注意事项和拆分实施计划。
5. 如发现关键不确定性，列出需要用户决策的问题。

只读调查；不要编辑文件、提交、推送或创建 PR。