项目：/root/code-space/blade-agent
目标：调查 GitHub PR https://github.com/blade-hq/blade-agent/pull/1932 在“导入”场景中是否必须使用新版镜像，还是旧的 2608.0.4 镜像也能顺利支持导入。

请使用 Codex 完整调查并给出结论，重点：
1. 阅读 PR 1932 的改动、相关代码、提交历史和仓库文档/测试；必要时查 GitHub PR 页面及镜像版本引用。
2. 明确导入流程依赖了哪些镜像内的能力、版本或文件；区分“代码层面要求”和“部署/运行时建议”。
3. 针对 2608.0.4 旧镜像判断是否能顺利导入；若不能，指出确切阻塞点和最低要求；若可以，说明条件与风险。
4. 如能验证，运行必要的只读检查/测试，但不要修改代码。

回报内容：结论（必须/可以）、证据对应到文件/代码/PR提交，必要的复现或验证结果，以及不确定性。不要改文件，不要创建 PR。
