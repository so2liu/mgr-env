继续当前模型回退任务，调查并修复以下问题：界面显示模型 `localds/deepseek-v4-flash-0731`，用户问其中 `local` 从哪里来的；如果是 hardcode，请去掉。

要求：
1. 定位 `localds/deepseek-v4-flash-0731` 及 `localds` 的来源，区分真实配置/默认值、测试 fixture、展示拼接逻辑和硬编码。
2. 如果该模型或 `local` 是产品代码中的硬编码，移除硬编码，改为使用系统配置/实际模型标识；不要破坏 provider/model 的合法解析。
3. 如果只是测试或环境配置，明确说明来源，并判断是否仍需修正用户可见展示。
4. 补充或修改回归测试，运行相关测试。
5. 当前模型回退改动已要求创建 PR 并 babysit；如本次属于同一变更，直接在现有分支/PR继续修改、推送并继续 babysit，不要合并。

回报根因、修改文件、测试结果、commit/PR 状态。
